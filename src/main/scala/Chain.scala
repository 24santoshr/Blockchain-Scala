/**
 * @author Santosh
 */

/*
import akka.http.scaladsl.marshallers.sprayjson.SprayJsonSupport
import spray.json.{DefaultJsonProtocol, JsonFormat}

trait ChainJsonSupport extends SprayJsonSupport with DefaultJsonProtocol {

  implicit val chainInfoFormat: JsonFormat[Chain] = jsonFormat4(Chain)
}

final case class Chain(index: Int,
                       hash: String,
                       values: List[Block],
                       timestamp: Long
                      )



*/